language: cpp
compiler:
  - clang
install:
- if [ "$CXX" = "g++" ]; then export CXX="g++-4.8" CC="gcc-4.8"; fi
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - libcppunit-dev
    - doxygen
    - clang
    - gcc-4.8
    - g++-4.8
script: make all && make test
